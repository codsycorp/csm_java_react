# Dev: Vite :3333 proxies /api → Go backend (localhost:9999)
# Production build dùng frontend-admin/.env (api.csmbridge.net)
VITE_API_BASE_URL=http://localhost:9999/api

# Local dev SEO: one-shot + Go backend qua proxy (auto-lmkt nhận localhost trong domain_api_url)
VITE_AI_SEO_ONE_SHOT=true
