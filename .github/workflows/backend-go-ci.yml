name: backend-go CI (manual only)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Manual deploy mode
        run: |
          echo "Automatic build/deploy is disabled."
          echo "Build artifacts manually via backend-go/docker-build.sh and deploy by hand."
